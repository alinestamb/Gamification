import {createTheme } from '@mui/material';
const Robotomono = createTheme({
    typography:{
        fontFamily: ['Roboto Mono','monospace'
        ].join(","),
    },
});
export default Robotomono; 

